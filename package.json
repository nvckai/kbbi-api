{
  "name": "open-api-kbbi",
  "version": "2.0.0",
  "main": "api/index.ts",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel deploy --prod",
    "prepare-data": "node scripts/prepare-data-vercel.js",
    "build": "echo 'Vercel handles builds automatically'",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "echo \"No linter configured yet\""
  },
  "keywords": ["kbbi", "indonesian", "dictionary", "api", "vercel"],
  "author": "baguskto",
  "license": "ISC",
  "description": "Indonesian Dictionary API powered by KBBI dataset - Vercel Edition",
  "dependencies": {
    "@vercel/node": "^3.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.5",
    "typescript": "^5.9.3",
    "vercel": "^33.3.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
